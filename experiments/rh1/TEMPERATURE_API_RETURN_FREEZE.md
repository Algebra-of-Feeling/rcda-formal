# Real API temperature return: exploratory bounded diagnostic

User authorized return to xAI API after CLI illustrations. Execute at most ten
stateless responses for the fixed soil_sensor_transfer scenario, role A and
authority probe from the existing v0.2 prompt fixture. Grok 4.6, high effort,
4096 output cap, 600-second client timeout. Temperature order fixed before
execution: 0.2, 0.7, 1.2, 1.7, 2.0, 2.0, 1.7, 1.2, 0.7, 0.2. No seed set.

Each call uses identical system/user messages without prior responses or CLI
simulation content. Verify returned model, temperature and effort. Stop on
first failure, mismatch, invalid score or budget limit; no automatic retries.
Report all attempted calls, scores by temperature and range, without significance
or equivalence claims. Two samples per level are exploratory, with coarse endpoint
and reused scenario. Not H-M1, not a latent-state experiment, not v0.2 execution.

Execution cap USD 0.50 is reallocated from the previous USD 2.50 v0.2 reserve;
USD 2.00 remains reserved while this diagnostic runs. Previous conservative
exposure USD 7.4187645407 plus both reservations is USD 9.9187645407. Return
unused diagnostic reservation after completion. Free API credits are user-reported,
not independently established: retain response cost ticks regardless of credit
coverage. Credentials are read locally through the already-authorized Notes
bridge, never saved or printed. Timeout retains uncertain cost reservation.
